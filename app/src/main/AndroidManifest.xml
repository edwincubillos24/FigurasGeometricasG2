<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.cubidevs.figurasgeometricasg2">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
            android:theme="@style/AppTheme">

        <activity android:name=".PerimetroRomboActivity"
                  android:label="@string/perimetro">
        </activity>

        <activity android:name=".PerimetroRectanguloActivity"
                  android:label="@string/perimetro">

        </activity>

        <activity android:name=".AreaRomboActivity"
                  android:label="@string/arearombolabel">
        </activity>

        <activity
                  android:name=".PerimetroPentegonoActivity">
        </activity>

        <activity android:name=".Pentagono_AreaActivity"
                  android:label="@string/SelAreaPentagono">
        </activity>

        <activity
                android:name=".squarePerimeterActivity"
                android:label="@string/square_perim_tag">
        </activity>

        <activity android:name=".SelAreaPerActivity"
                  android:label="@string/seleccion">
        </activity>

        <activity android:name=".AreaTrianguloActivity"
                  android:label="@string/areaTriangulo">

        </activity>

        <activity
                android:name=".SquareAreaActivity"
                android:label="@string/titlesquarearea">
        </activity>

        <activity
                android:name=".areCirculoActivity"
                android:label="@string/areacirculo">
        </activity>

        <activity android:name=".AreaRectanguloActivity">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>